// See https://containers.dev/implementors/json_reference/ for configuration reference
{
	"name": "Codespace configuration for frontend",
	"build": {
		"dockerfile": "Dockerfile"
	},
  "remoteUser": "node"
}
